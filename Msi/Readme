algumas informações que podem ser úteis

host = "localhost",
user = "root",
passwd = "1234",
database = "msi"

comando pra adicionar o arquivo mysqldump.sql no banco de dados

mysql -u root -p <banco de dados>  <  <caminho do arquivo>\mysqldump.sql
